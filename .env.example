# Copy this file to .env and paste your own key in. .env is gitignored and never
# leaves your Mac -- the key is used only while building the film pool, and the
# finished app carries no key at all.
TMDB_API_KEY=
